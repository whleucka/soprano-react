import NavLinks from "./NavLinks";

const Sidebar = () => {
    return (
        <nav id="sidebar">
            <h4 className="brand">Soprano</h4>
            <NavLinks />
        </nav>
    );
};

export default Sidebar;
