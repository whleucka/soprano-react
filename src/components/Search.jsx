import SearchInput from './SearchInput';
import SearchResults from './SearchResults';

const Search = () => {
    return (
        <section id="search">
            <h2>Search</h2>
            <SearchInput />
            <SearchResults />
        </section>
    );
};

export default Search;
