import SearchInput from './SearchInput';

const Search = () => {
    return (
        <section id="search">
            <h2>Search</h2>
            <SearchInput />
        </section>
    );
};

export default Search;
